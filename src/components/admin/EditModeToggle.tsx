
import React from 'react';

// This is a placeholder component since edit mode has been removed
const EditModeToggle: React.FC = () => {
  return null; // Doesn't render anything
};

export default EditModeToggle;
